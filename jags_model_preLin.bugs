model{
    for(i in 1:N){
        y[i] ~ dnorm(mu[i], tau)
        mu[i] <- Beta0 + Beta1 * x1[i] + Beta2 * x2[i]
    }

    Beta0 ~ dnorm(2.5, 0.2)
    Beta1 ~ dnorm(0.0, 0.2)        
    Beta2 ~ dnorm(0.0, 0.2)

    tau ~ dgamma(.001, .001)
}
