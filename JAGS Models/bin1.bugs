model{
    for(i in 1:N){
        ystar[i] = Beta0 + Beta1 * x1[i] + Beta2 * x2[i]
        logit(p[i]) = ystar[i]
        y[i] ~ dbern(p[i])
    }

    Beta0 ~ dnorm(0, 0.01)
    Beta1 ~ dnorm(0, 0.01)
    Beta2 ~ dnorm(0, 0.01)

}
